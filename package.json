{
  "name": "linqpad-affiliate-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for linqpad affiliate",

  "scripts": {
    "build:client": "cd client && pnpm build",
    "build": "pnpm run build:client",
    "dev:client": "cd client && pnpm dev",
    "dev": "pnpm run dev:client"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {}
}
